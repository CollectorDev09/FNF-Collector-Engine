on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: [windows]
    steps:
      - uses: krdlab/setup-haxe@v1
        with:
          haxe-version: 4.3.7
      - run: |
          haxe -version
          haxelib install hxnodejs